import { AppState } from "../app.state";

export const fetchProfile = (state: AppState) => state.profile;

// export const fetchUserDetails = createSelector(
//   fetchClass,
//   (state: AppState) => state.profile.userDetails
// );
